# Практическая работа: ADO.NET  

📌 В качестве предметной области используйте классы из предыдущей работы.  

---

### Задание 0  
С помощью средств **ADO.NET** создать две таблицы в базе данных:  
- **Manufacturer** (производитель);  
- **Product** (Phone / Tank / Car и т. д., в зависимости от варианта).  

Установить связь **один-ко-многим**:  
- 1 `Manufacturer` → много `Product`.  

---

### Задание 1  
Создать класс, который **асинхронно** заполняет таблицы данными:  
- 30 записей для каждой таблицы;  
- после добавления записи в таблицу `Manufacturer` сразу добавлять запись в таблицу `Product`.  

---

### Задание 2  
Добавить логику, которая позволит:  
- добавлять новые записи в таблицу `Manufacturer`;  
- добавлять новые записи в таблицу `Product`.  

---

### Задание 3  
Реализовать выборку, которая позволяет получить **все записи продуктов для указанного производителя (Manufacturer)**.  

---

✅ В результате выполнения у вас должно получиться приложение, которое:  
- создаёт структуру таблиц в БД с отношением «один-ко-многим»;  
- асинхронно заполняет их начальными данными;  
- поддерживает добавление новых данных;  
- позволяет делать выборку продуктов по конкретному производителю.  
